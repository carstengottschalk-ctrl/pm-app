# Supabase Configuration (Required for production)
# Get these values from your Supabase project settings > API
NEXT_PUBLIC_SUPABASE_URL=https://your-project-id.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

# Supabase Service Role Key (Optional - for server-side operations)
# Only use this in secure server environments, never expose in browser
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# Sentry Error Tracking (Optional - recommended for production)
# Get these values from your Sentry project settings > Client Keys (DSN)
SENTRY_DSN=https://xxx@xxx.ingest.sentry.io/xxx
NEXT_PUBLIC_SENTRY_DSN=https://xxx@xxx.ingest.sentry.io/xxx
# SENTRY_AUTH_TOKEN=sntrys_xxx  # Required for source maps upload

# Email Configuration (Optional - for production email features)
# SMTP_HOST=smtp.sendgrid.net
# SMTP_PORT=587
# SMTP_USER=apikey
# SMTP_PASS=your-sendgrid-api-key
# EMAIL_FROM=noreply@yourdomain.com

# Application URL (Required for auth callbacks)
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Security (Optional)
# SESSION_SECRET=your-random-secret-key-here
